# UI Spec (Source of Truth)

UI designs are maintained in Pencil.ai and must be fetched via MCP.

Frontend tasks must:

- Pull latest design for the relevant screens via MCP
- Follow the layout, spacing, typography, and components exactly
- Implement loading/empty/error states as shown (or document assumptions)

Design location:

- Pencil Project: <YOUR PROJECT ID OR NAME>
- Screens: Auth, Collections, Feeds, Articles, Rules
